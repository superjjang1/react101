class CardClass extends React.Component {
    //in order for this to be a component, that it's a component from react, add extends
    // React.Component
    constructor (){
        super()
        //every class component must have a render method
    }
    render(){
        return <h1>Sanity Check!</h1>
    }
}